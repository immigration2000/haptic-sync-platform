# PULSE 플랫폼 — 작업 규칙

이 저장소는 **PC의 Claude Code** 담당이다.
데스크톱 릴레이 앱(`haptic-relay-desktop`)은 **노트북의 Codex** 담당이며 PC에서 건드리지 않는다.

공유 노트 저장소: `C:\Users\user\AI_NOTES` (원격: https://github.com/immigration2000/AI_NOTES.git)

---

## 0. 작업 흐름 (필수)

```
① AI_NOTES 확인  →  ② 작업  →  [방향 바뀌면 ③ 멈추고 보고 → 수락 → 재개]
   →  ④ 플랫폼 저장소에 기록  →  ⑤ AI_NOTES 재확인(pull)  →  ⑥ AI_NOTES에 기록 → push
```

### ① 작업 전 — 원격 상태 확인 (두 저장소 모두)

```bash
# 1) 작업 대상 저장소가 원격보다 뒤처졌는지 먼저 본다
git fetch && git status -sb        # "behind N"이면 pull 먼저

# 2) 공유 노트
cd C:\Users\user\AI_NOTES && git pull --rebase
```

`RULES.md` · `logs/pc.md` · `logs/laptop.md`를 읽고 이번 작업 관련 주의사항을 파악한다.

⚠ **`git log`만 보고 현재 상태로 단정하지 말 것.** 로컬이 뒤처져 있을 수 있다.
2026-08-18에 이걸 안 해서 **이미 원격에 갱신돼 있던 문서를 낡은 것으로 오판하고
처음부터 다시 만든** 사고가 있었다. 로컬 checkout ≠ 저장소 최신 상태.

**적용 범위: 코드나 문서를 바꾸는 작업만.** 단순 조회·질문("이 파일 뭐야?", "어디에 있어?")에는
붙이지 않는다. 매번 pull하면 낭비다.

### ③ 방향이 바뀌면 멈추고 보고한다

**멈춘다:**
- DB 스키마·마이그레이션을 건드려야 할 때
- 기존 설계 원칙과 충돌할 때 — SSOT(`server/service_types.js`), `publicUrl()` 경유,
  `CORE_BLOCK`(법적 안전장치)
- 두 저장소 간 계약에 영향 — T-Code 포맷, WebRTC 데이터채널 프로토콜
- 예상 범위를 크게 벗어날 때 (한 곳 고치려다 5곳 이상 연쇄)
- 요청받은 것과 **다른 게 진짜 원인**일 때

**안 멈춘다:** 같은 목표 안에서 구현 방법만 바꾸는 것 / 버그 발견 즉시 수정 / 오타·경로 수정

### ④ 플랫폼 저장소에 기록 — 임계값을 지킬 것

`PROJECT_STATUS.md`는 이미 400줄이 넘는다. **매 작업마다 §6-N을 추가하면 못 읽는 문서가 된다.**

| 작업 성격 | 기록 위치 |
|---|---|
| 구조가 바뀐 작업 (재편·신규 시스템) | `PROJECT_STATUS.md` §6-N **신설** |
| 기능 추가·버그 수정 | §8 TODO 체크 + 필요하면 §7 트러블슈팅 한 줄 |
| 오타·경로·문구 | 기록 안 함 |

문서와 코드가 어긋난 걸 발견하면 **그 자리에서 문서를 고친다.** (문서가 낡아 사고 난 이력이 있다)

### ⑤ 기록 직전 AI_NOTES를 **다시** pull 한다
작업이 길어지면 시작 때 읽은 정보가 낡는다. 실제로 2026-08-18에 이걸 안 해서
노트북이 그 사이 올린 내용을 못 보고 **틀린 규칙(R-4)을 썼다가 바로 정정**한 사고가 있었다.
기록 전 재확인은 잘못된 정보가 상대에게 전달되는 것을 막는다.

### ⑥ AI_NOTES에 기록
`logs/pc.md` **맨 위에** `## [YYYY-MM-DD HH:MM] 요약` 형식으로 추가한다.
→ **이 기기(PC)는 `logs/pc.md`에만 쓴다. `laptop.md`는 절대 건드리지 않는다.**
두고두고 지킬 규칙이면 `RULES.md`에도 반영한다.

```bash
cd C:\Users\user\AI_NOTES && git add -A && git commit -m "note: 요약" && git pull --rebase && git push
```

무엇을 쓸 것인가 — **상대가 모르면 사고 나는 것** 위주로. 내가 한 일의 나열이 아니라
함정·결정사항·정정. 내 이전 기록이 틀렸으면 **정정을 명시적으로 남긴다.**

---

## 1. 커밋 정책

- **문서 변경(`.md`)** → 자동 커밋. 확인 안 받아도 된다.
- **코드 변경** → 커밋 전 사용자 확인.
- `haptic-relay-desktop`은 **커밋하지 않는다.** (노트북 담당)

---

## 2. 시작할 때 읽을 것

`PROJECT_STATUS.md` **§0을 먼저** — §6-1~§6-10은 2026-06-10 기록이고 §6-8은 폐기됐다.
주제별로 어느 코드 파일을 신뢰할지 §0 표에 정리돼 있다. **코드와 문서가 다르면 코드가 맞다.**
